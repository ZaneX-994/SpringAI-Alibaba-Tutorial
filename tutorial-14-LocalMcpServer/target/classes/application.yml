server:
  port: 8014

  servlet:
    encoding:
      enabled: true
      force: true
      charset: UTF-8

spring:
  application:
    name: SAA-14-LocalMcpServer

  ai:
    dashscope:
      api-key: ${aliQwen_key}

    # MCP Server Config
    mcp:
      server:
        type: async
        name: customer-define-mcp-server
        version: 1.0.0


