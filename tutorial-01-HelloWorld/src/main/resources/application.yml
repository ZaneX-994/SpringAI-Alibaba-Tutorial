server:
  port: 8001

  servlet:
    encoding:
      enabled: true
      force: true
      charset: UTF-8

spring:
  application:
    name: SSA-01-HelloWorld

  # SpringAIAlibaba config
  ai:
    dashscope:
      api-key: ${aliQwen_key}
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      chat:
        options:
          model: qwen-plus

